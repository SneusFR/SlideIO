# SlideIO frontend — environment variables (Vite)
# Copy to .env.local for overrides. Defaults to ws://localhost:2567 if unset.

# WebSocket URL of the Colyseus multiplayer server.
# Production example (later): wss://multiplayer.slideio.example
VITE_MULTIPLAYER_SERVER_URL=ws://localhost:2567