# SlideIO multiplayer server — environment variables
# Copy to .env and adjust if needed. No secrets required for Phase 1.

# HTTP + WebSocket port (Colyseus default)
PORT=2567

# Allowed frontend origins for matchmaking requests (comma-separated).
# Add the production domain later, e.g.: https://slide-io.vercel.app
CORS_ORIGIN=http://localhost:5173